# Copie para .env e preencha. NÃO comite o .env (já está no .gitignore).

# Banco MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=sinistros_cabure

# Servidor
PORT=3000
SESSION_SECRET=troque-este-segredo-por-uma-string-aleatoria-longa

# A extração de campos é 100% local (Tesseract OCR + regex + leitura de Dataprev).
# Nenhuma chave de API é necessária.
